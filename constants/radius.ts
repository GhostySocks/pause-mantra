/**
 * Pause Mantra — Border Radius System
 */

export const Radius = {
  pill: 999,       // Buttons, chips, tier strip, progress bar
  card: 20,        // Mantra cards
  tile: 16,        // Stat tiles, calendar card
  sheet: 24,       // Modal sheet top corners
  input: 12,       // Search bar, textarea, app rows
  appIcon: 9,      // App list icons
  calendar: 8,     // Calendar cells
  progressBar: 2,  // Progress bar segments
  dragHandle: 2,   // Modal drag handle
} as const;
